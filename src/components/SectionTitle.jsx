import React from "react";

const SectionTitle = ({ header }) => {
  return (
    <div>
      <h2 className="text-center text-5xl font-bold mb-10">{header}</h2>
    </div>
  );
};

export default SectionTitle;
